# Update

这是在 3.6 竞技版那会还没有完整逆向原工程重建时候，为了实现一个简易的更新检查器，功能就是单纯的访问一个放在 git 远端仓库的 json 文件，然后暴力直接比对版本是否一致（我甚至懒得做主次版本比较），不一样直接报有更新。当然这东西实质是一个装逼练手的东西（炫耀 Java 但很明显我还不是那块料），并且由于编译出来后是一个 jar 文件，为了防止用户电脑没带 java 无法运行我还 “贴心” 的附赠了一个 jre 大礼包，因此这玩意被后来开智的我愤而砍掉（不是）。
