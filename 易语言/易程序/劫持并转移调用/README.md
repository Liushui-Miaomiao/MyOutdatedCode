# 劫持并转移调用

最初是给我的一个老笔记本里单独特供的一个程序，用于劫持 Chrome 的默认执行参数，因为如果单单修改快捷方式中的路径后面那一块的话，像通过双击 html 文件或者打开链接时候拉起的浏览器就不会生效，因此我为了劫持 chrome.exe（Chrome 主程序），做了个劫持程序，只要名称是 chrome.exe 的程序就会被我的傀儡程序劫持添加指定参数后再启动，这样就实现了静默修改启动参数。这个程序通过修改 exe 映像名也可以用来劫持其他程序，缺点是只要名称是这个名的程序都会被劫持。
